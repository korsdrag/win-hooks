# add_library(win-winlib)

# set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# target_compile_features(win-winlib PUBLIC cxx_std_23)
# target_compile_options(win-winlib PUBLIC /W4 /WX /Debug /Od)

# target_link_libraries(win-hack PUBLIC -lstdc++exp)
# target_link_libraries(win-hack PUBLIC win-winlib)

